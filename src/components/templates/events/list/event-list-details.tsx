export function EventListDetails() {
  return (
    <>
      {/* {groupItems === "grid" &&
        response.data.map((event) => (
          <EventCardGrid key={`${event.id}-${event.timestamp}`} event={event} />
        ))}
      {groupItems === "list" &&
        response.data.map((event) => (
          <EventCardList key={`${event.id}-${event.timestamp}`} event={event} />
        ))} */}
    </>
  );
}
